/**
 * File thực hành I.1 (TypeScript - Interface).
 * Dùng trong component: import { Product } from '../practice/models/product.model';
 */
export interface Product {
  id: number;
  name: string;
  price: number;
}
